import {
    StyleSheet,
  } from "react-native";
  
  export const styles = StyleSheet.create({
    commentItem: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 10,
    },
    commentText: {
        marginLeft: 10,
        color: "white"
    },

    userAvatar: {
        width: 40,
        height: 40,
        borderRadius: 20,
        marginRight: 10,
        marginLeft: 8,
    },
  })
